package pl.mariuszk.chapter_3_making_decisions;

public class SwitchExpression {

    public SwitchExpression() {
        /*
        In the below example, the first branch is a case expression. It requires using semicolon at the end.
        The second branch is a case block which requires curly braces and a 'yield' statement if switch returns a value.
        The default branch is required when switch expression returns a value and all possible case statement values are not handled.

        int result = switch (variableToTest) {
            case constantExpression1 -> 5;
            case constantExpression2, constantExpression3 -> {
                yield 10;
            }
            default -> 20;
        }
         */
    }
}
